FROM python:3.12-slim

WORKDIR /app

RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

COPY Backend/ ./Backend/

RUN pip install flask flask-cors qrcode[pil] pillow

EXPOSE 5000

CMD ["python", "Backend/app.py"]
